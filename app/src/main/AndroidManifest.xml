<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ripigo.gamesuitebinar">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GameSuiteBinar">
        <activity android:name=".ui.gameplay.GamePlayerActivity"></activity>
        <activity android:name=".ui.gameplay.GameComActivity" />
        <activity android:name=".ui.gameplay.GameActivityVsPlayer" />
        <activity android:name=".ui.menu.MenuActivity" />
        <activity android:name=".ui.splashscreen.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.intro.IntroSliderActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".ui.gameplay.GameActivityVsCom" />
    </application>

</manifest>